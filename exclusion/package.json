{
  "private": true,
  "workspaces": {
    "packages": [
      "app1",
      "app2",
      "react-launch-line"
    ]
  },
  "scripts": {
    "start": "lerna run --parallel start",
    "build": "lerna run build",
    "serve": "yarn clean && yarn build && lerna run --parallel serve",
    "clean": "lerna run --parallel clean"
  },
  "devDependencies": {
    "lerna": "3.22.1"
  }
}
